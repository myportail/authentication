apiVersion: v1
kind: Service
metadata:
  name: authservice
  namespace: {{ .Values.namespace }}
  labels:
    app: myportail
    service: authservice
spec:
  ports:
    - port: 80
      name: authservice-http
      targetPort: 80
    - port: 443
      name: authservice-https
      targetPort: 443
  selector:
    app: myportail
    service: auth
